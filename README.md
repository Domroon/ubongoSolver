# A simple program to solve a ubongo game

## Setup Windows

    python -m venv venv
    ./venv/Scripts/activate
    pip install pygame
    python main.py

## Setup Linux

    python -m venv venv
    source venv/bin/activate
    pip install pygame
    python main.py
